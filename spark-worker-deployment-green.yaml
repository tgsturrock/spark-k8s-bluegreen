apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-worker-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spark
      role: worker
      version: green
  template:
    metadata:
      labels:
        app: spark
        role: worker
        version: green
    spec:
      containers:
      - name: spark-worker
        image: tgsets/spark:3.2.1
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
        command: ["/bin/sh", "-c", "/opt/spark-worker"]
        resources:
          requests:
            memory: "1Gi"
            cpu: "1"
          limits:
            memory: "2Gi"
            cpu: "2"
